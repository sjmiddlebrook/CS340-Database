<h1>Update {{event.name}}:</h1>
<form id="update-event" action="/events" method="post">
    <div class="form-group">
        Name: <input type="text" name="name" value="{{event.name}}"><br>
    </div>
    <div class="form-group">
        Setting: <input type="text" name="setting" value="{{event.setting}}"><br>
    </div>
    <div class="form-group">
        Characters involved:<br>
        {{#each characters}}
            <input type="checkbox" name="characters" id="{{id}}" value="{{id}}"/>
            <label for="{{id}}">{{name}}</label><br>
        {{/each}}
        </select><br>
    </div>
</form>

<button class="btn btn-primary" onclick="updateEvent({{event.id}})">Update</button>
{{#each eventCharacters}}
<script defer>selectCharacters({{character_id}});</script>
{{/each}}